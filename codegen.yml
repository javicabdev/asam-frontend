overwrite: true
schema:
  - http://localhost:8080/graphql
documents:
  # Solo buscar en archivos .graphql para evitar duplicados
  - src/graphql/operations/**/*.graphql
  - src/graphql/fragments/**/*.graphql
generates:
  # Generar tipos del schema
  src/graphql/generated/schema.ts:
    plugins:
      - typescript
    config:
      enumsAsTypes: true
      scalars:
        Time: string
        JWT: string
        ID: string
  
  # Generar tipos de operaciones y hooks
  src/graphql/generated/operations.ts:
    plugins:
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
      withComponent: false
      withHOC: false
      enumsAsTypes: true
      scalars:
        Time: string
        JWT: string
        ID: string
  
  # Generar archivo de introspección para herramientas
  src/graphql/schema.json:
    plugins:
      - introspection
